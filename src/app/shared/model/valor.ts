export class Valor {
  id: number;
  nombre: string;
  definicion: string;

  constructor(id: number, nombre: string, definicion: string) {
    this.id = id;
    this.nombre = nombre;
    this.definicion = definicion;
  }
}
